machine:
  node:
    version: 6.2.2
deploy:
  override:
    - npm install --no-progress
    - ./node_modules/.bin/slate-tools zip
    - ejson decrypt -o secrets.staging.json secrets.staging.ejson
    - node scripts/build-src
    - node scripts/deploy --staging
