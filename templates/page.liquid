<!-- /templates/page.liquid -->
<div class="grid">
  <div class="grid__item">

    <h1>{{ page.title }}</h1>

    <div class="rte">
      {% comment %}
        Regular page content will appear here.
      {% endcomment %}
      {{ page.content }}
    </div>

  </div>
</div>
